#include <bits/stdc++.h>
using namespace std;
#define int long long

void solve(int arr[],int n,int m){
    
}

signed main(){
    int n,m;
    cin>>n>>m;
    int arr[n];
    for(int i=0;i<n;i++){
        cin>>arr[i];
    }
    sort(arr,arr+n);
    solve(arr,n,m);
}